/**
 * LEGACY one-off — do not run. Schema is in server/migrations/.
 * Historical: admin grading columns on user_courses + role default.
 */
console.error('[Migration 001] DEPRECATED — use server/migrations/ via npm run migrate');
process.exit(1);

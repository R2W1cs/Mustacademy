{
  "name": "mustacademy-platform-v20",
  "version": "20.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install-all": "cd frontend && npm install && cd ../server && npm install"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}

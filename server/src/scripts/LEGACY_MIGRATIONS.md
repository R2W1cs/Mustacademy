# Legacy one-off migration scripts

Files named `migration_*.js` in this folder are **historical**. They are not part of boot or CI.

Canonical schema path: `server/migrations/` + `npm run migrate`.

Optional content seeds (not schema):

- `npm run seed:course-careers`
- `npm run seed:lesson-content`

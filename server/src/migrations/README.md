# LEGACY — do not run

These one-off scripts mutated production over time. Schema truth now lives in `server/migrations/` (node-pg-migrate).

Kept only for historical reference. New DDL must go in `server/migrations/`.
